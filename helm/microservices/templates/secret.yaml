apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: {{ include "microservices.namespace" . }}
  labels:
    app: postgres
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.secret.postgresPassword | quote }}
  PGPASSWORD: {{ .Values.secret.postgresPassword | quote }}
